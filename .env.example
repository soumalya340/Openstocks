# Copy to .env and adjust as needed:
#   cp .env.example .env

PORT=3000
DB_PATH=./data/openstocks.sqlite
JWT_SECRET=change-me-in-production
# Global HTTP rate limit (per IP across all routes)
THROTTLE_TTL_MS=60000
THROTTLE_LIMIT=100



